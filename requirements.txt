# 纯Python依赖
Pillow>=9.0.0
tkinterdnd2>=0.3

# 注意：ffmpeg 需手动安装（非Python包）：
# Windows：下载ffmpeg并添加到系统PATH；
# Linux：sudo apt install ffmpeg；
# Mac：brew install ffmpeg

# 安装命令：
<<<<<<< HEAD
# pip install -r requirements.txt
=======
# pip install -r requirements.txt

# 打包命令示例（需先安装PyInstaller）：
# python -m PyInstaller --onefile --windowed --hidden-import=tkinterdnd2 --name MC资源处理工具 --icon=icon.ico .\run.py
>>>>>>> 19cb6e6 (Release-0.1)
